# 关于最优期望通关天数

**规则**：初始通关概率 0；每天 3 次提升，每次 +2%；可随时以当前概率尝试通关，次数无限；失败则概率减半向下取整。求最优期望通关策略。

## 问题

某关卡初始通关率 `0%`。每天你可以：

- **提升**：通关率 `+2` 个百分点，每天最多 3 次
- **尝试**：按当前通关率赌一次，成功了就通关；失败了通关率**折半**
- **收工**：结束这一天，明天的 3 次提升额度恢复

问：**平均最快几天通关？**

答案：`649/73 ≈ 8.8904` 天。

---

## 直觉为什么错

**误区一：存在"多少概率以上就该尝试"的阈值。** 这是最自然的猜想，也是错的。真实最优策略在 `r = 0`（当天已用完提升）层确实有阈值 `τ₀ = 8`——概率 ≥ 8% 必尝试；之下是否尝试**非单调**（误区二就是证据）。但**一旦当天还有提升次数，阈值就崩了**：

| 剩余提升次数 r | 尝试（T）最优的最高 p | 阈值存在？ |
|---|---|---|
| 0 | p ≥ 8 全尝试 | ✅ 有（τ₀ = 8） |
| 1 | 最高 p = 61，但 p = 62 处又变回提升 | ❌ |
| 2 | 最高 p = 25 | ❌ |
| 3 | 最高 p = 17 | ❌ |

`r = 1` 层的 T 态分布是 `(1,1), (4,5), (8,9), (12,13), (16,17), (20,21), (24,29), (36,37), (44,45), (56,57), (60,61)`——**锯齿状，不是一个区间**。甚至有 `p = 24…29` 连续 6 个点都尝试，而旁边的 `p = 30…35` 全部回去提升。

**误区二：那应该就是"概率低就提升、高了就尝试"。** 也不对。`r = 0` 层 `p = 1..7` 的实际序列是 `T,E,E,T,T,E,E`——`p=4,5` 尝试最优，`p=6,7` 反而收工最优。**非单调**。

---

## 为什么"贪心"救不了你

直觉策略大概是"先憋到高概率再开赌"。但这里有个反直觉的结构：**失败折半不是"惩罚"，而是"回退到更好的位置"**——折半后你还有当天的提升次数，可以立刻提升上去。所以"早尝试"未必吃亏，"憋高"也未必划算。

最优解必须在**全部 400 个状态**上同时决定，无法用一条规则概括。这就是为什么需要 MDP ，而不是一条经验法则。

---

## 怎么验

**① 建模** — 状态 `(p, r)`，动作 {提升 U，尝试 T，收工 E}，目标是最小化期望自然日 `D = V + 1`。

**② 求解** — 浮点值迭代找到候选策略 → 精确有理数求值消除浮点误差。

**③ 验证** — 对全部 400 个状态逐一检验 Bellman 等式：

